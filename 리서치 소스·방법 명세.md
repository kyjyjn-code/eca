# EC 리서치 — 소스·방법 명세 (점검용)

> **작성 목적:** 외부 점검을 받기 위해 현재 운영 중인 리서치 방식을 있는 그대로 적는다.
> **작성 기준:** 2026-08-27, 저장소 실제 파일과 실측 데이터. 설계 의도가 아니라 **지금 돌아가는 것**을 적었다.
> **점검자에게:** §7 이 제가 직접 재서 찾아낸 문제들입니다. 여기부터 보셔도 됩니다.

---

## 1. 이 시스템이 하는 일

한국 거주 초·중·고 학생(검정고시·대안경로 포함, 미국 입시 준비)이 지원할 수 있는
**대회·공모전·프로그램(ECA)** 을 매월 모아, 학생이 직접 조건을 걸러 볼 수 있는
공개 사이트로 게시한다.

- 공개 사이트: `https://kyjyjn-code.github.io/eca/`
- 데이터 원천: `data/master.json` 하나 (동적 마스터, 매월 누적·갱신)
- 게시물: `docs/index.html` + `docs/data.json` (마스터에서 생성)

### 파이프라인 9단계

| # | 단계 | 담당 | 산출물 |
|---|---|---|---|
| 1 | 리서치 | **LLM 에이전트** | `data/raw/YYYY-MM.json` |
| 2 | 큐레이션 | **LLM 에이전트** | `data/curated/YYYY-MM.json` |
| 3 | 병합 | 스크립트 `merge.js` | `data/master.json` + 병합 리포트 |
| 4 | 링크 검증 | 스크립트 `check-links.js` | 게시상태 갱신 + 리포트 |
| 5 | 포스터 | 스크립트 `fetch-posters.js` | `docs/posters/` |
| 6 | 엑셀 | 스크립트 `export-excel.js` | `records/*.xlsx` |
| 7 | 빌드 | 스크립트 `build.js` | `docs/` |
| 8 | 변경 요약 | 스크립트 `pr-summary.js` | `pr-body.md` |
| 9 | 가드레일 | 스크립트 `check-guardrails.js` | 통과/중단 |

**설계 원칙: 판단이 필요한 일만 LLM 이 한다.** 1·2 단계만 에이전트이고 나머지는 결정적 스크립트다.
단계 간 연결은 항상 JSON 파일이라 어느 지점부터든 재실행할 수 있다.

**에이전트 권한 분리**
- `researcher`: 웹검색·WebFetch, `data/raw/` 쓰기만. 큐레이션·git·게시 금지.
- `curator`: `data/raw/` 읽기, `data/curated/` 쓰기만. **웹검색 금지**(사실을 새로 만들지 못하게).

---

## 2. 소스 전략 — 3층 구조

`data/sources.json` 에 정의. 씨앗 URL 은 **시작점일 뿐**이고, 매월 공식 페이지에서 실제 정보를 재확인한다.

### 1층 — 핵심 씨앗 (거의 안 변함, 총 46개)

분류별 등록 수: 과학·공학·수학생명·AI **15** / 글쓰기·논문·인문사회 **6** /
영어(과목태그) **6** / 국제·리더십·사회참여 **5** / 환경·예술·공익 **4** /
스포츠·문화·봉사 **4** / 경제·비즈니스 **3** / 선발형 여름 프로그램 **3**

대표 항목: KMO·KPhO·KChO·KBO·KOI(국내 올림피아드 5종), AMC, USACO, 전국과학전람회,
국제수학캥거루, 천문올림피아드 / John Locke Essay, The Concord Review, 보훈학술논문공모전 /
NEC, IEO, Wharton Investment Competition / RSI, SSP, Ross Mathematics Program /
NSDA, World Scholar's Cup, TOEFL Junior / MUN 계열, UN청소년환경총회 등

### 2층 — 발굴 채널

**(a) 정기 순회 소스** — 매월 WebFetch 로 직접 순회하는 허브 5곳

| 채널 | URL | 용도 |
|---|---|---|
| e청소년 (여성가족부) | youth.go.kr | 청소년 프로그램·공모 공고 |
| 1365 자원봉사포털 | 1365.go.kr | 봉사·공익 활동 |
| 대한수학회 | kms.or.kr | 수학 대회 공식 공고 |
| 한국과학창의재단 | kofac.re.kr | 과학·SW 대회·전람회 |
| 환경부 공모·알림 | me.go.kr | 환경 공모전 |

**(b) 검색 쿼리 세트** — 대분류별 국문·영문 병행, 연도(YYYY) 롤링 치환.
겨울철(11~2월)에는 다음 연도까지 함께 검색. 예:
`"YYYY 청소년 과학 대회 접수"`, `"YYYY 중고등 수학 올림피아드 요강"`,
`"youth YYYY economics competition Korea eligible"`,
`"YYYY high school essay competition international"`

**(c) 서브그리게이터(보조만)** — 온라인·콘테스트·대티즌. 대학생·마케팅 공모 위주라 핵심 소스로는 부적합.

**(d) 시즌 캘린더** — 월별로 "이번 달에 열리거나 임박해야 할 활동"을 능동 점검.
예: 1월 RSI 마감 임박·SSP 마감 / 2월 미국 여름프로그램 마감 집중·AMC 시즌 시작 /
6~7월 John Locke 접수·마감 / 8월 Wharton / 11~12월 차년도 여름프로그램 공고.

### 3층 — 검증 필터 (하나라도 불충족이면 배제)

| 필터 | 기준 |
|---|---|
| 주최성 | 정부·부처·공공기관·학회·대학·국제 공인단체면 통과. 개인·불명확은 배제 |
| **지원자격** | 한국 거주·검정고시·대안경로 학생이 **실제 지원 가능한지** 국적·거주 요건 확인. 근거를 `지원자격_확인` 칸에 남긴다 |
| 규모·역사 | 전국 규모 또는 회차 누적(제N회)이면 가점 |
| 상업성 경계 | 참가비만 비싸고 주최 공신력이 불분명하면 배제 |
| 출처 | 공식 URL 없으면 배제. 접수가 이메일뿐이면 공고 페이지를 찾아 연결 가능할 때만 |
| 만료 | 마감일이 이미 지난 활동은 배제 |

**명시적 배제 목록도 관리한다** — GSA 영어 말하기 대회·영어성경에세이대회(공신력 불명확·고비용),
주니어카이스트 교육캠프(수강생 한정이라 지원자격 미달).
**개별 확인 필요**로 보류 중: IJAC 청소년 국제 학술논문대회, 전국 중학생 뉴스저작권 토론대회, 대한민국청소년기자대상.

---

## 3. 리서치 방법 (`researcher` 에이전트)

### 증분 수집 — 매월 전량 재조사하지 않는다

먼저 `master.json` 을 읽어 이미 있는 활동을 파악하고, **이번에 꼭 필요한 것만** 조사한다.

**조사 대상**
1. 마스터에 없는 **새 활동** (2층 발굴)
2. 마감일이 **30일 이내로 임박**한 활동 → 요강 변경 가능성
3. 게시상태가 **`제외`** 이거나 확인 필요 메모가 있는 활동 → 되살아났는지

**재조사 안 함:** 마감이 30일보다 멀거나 상시(null)이면서 정상 게시 중인 기존 활동.
`merge.js` 가 삭제하지 않으므로 마스터에 그대로 남고, 링크 생존은 4단계가 매월 확인한다.

**분기 전량 재검증:** 1·4·7·10월에는 30일 밖·상시 활동까지 전 항목 공식 페이지를 한 번씩 재확인한다.
요강·대상·자격·회차가 조용히 바뀐 것을 잡기 위한 안전판.

### 사실만 기록한다 — 판정은 안 한다

리서처가 채우는 칸: `id`, `활동명`, `분야`, `대상_원문`, `지원자격_확인`, `주최`,
`신청기간_원문`, `근거_원문`, `주요일정`, `비용_원문`, `핵심내용`, `웹사이트`, `포스터_url`, `수집월`

**비워 두는 칸** (큐레이터·스크립트 담당): `과목태그`, `키워드`, `대상_학년`, `마감일`,
`비용_구분`, `미국입시_관련도`, `SDC_적합도`, `포스터`, `갱신일`, `게시상태`

### 환각·오염 방지 장치

- **`근거_원문` 필수** — 마감일·대상을 확인한 공식 페이지의 문구를 **원문 그대로** 기록.
  근거 문구를 찾지 못한 활동은 **수집하지 않는다.**
- **연도 캐시 오염 주의** — WebFetch 가 작년 페이지를 올해로 오인할 수 있다.
  연도가 명시된 문구를 우선 채택하고, 불명확하면 `[확인수준: 연도불명]` 을 근거_원문 끝에 붙인다.
- **확인수준 표기** — 요강 PDF 원문까지 봤으면 그대로, 공고 요약만 봤으면 `[확인수준: 공고요약]`.
  스키마 불변 원칙 때문에 **새 칸을 만들지 않고 기존 텍스트에 덧붙인다.**
- **id 에 수집월을 넣지 않는다** — 같은 활동이 다음 달에 재수집돼도 같은 id 라 자동 갱신된다.

### 자기 점검 (저장 직전 필수)

1. 대분류 5개 중 신규·재확인 0건인 곳이 있는가 → 있으면 해당 쿼리 세트를 한 번 더
2. 시즌 캘린더에서 이번 달에 떠야 할 활동이 빠지지 않았는가
3. 1층 대표 대회(AMC·KMO·John Locke 등) 중 시즌상 열려야 하는데 누락된 것은 없는가
4. 결과를 `data/raw/YYYY-MM.coverage.md` 에 남긴다

---

## 4. 큐레이션 방법 (`curator` 에이전트)

웹을 보지 않는다. 리서처가 남긴 사실에만 근거해 판정한다.

| 칸 | 판정 방법 |
|---|---|
| `대상_학년` | `대상_원문` → `초`/`중`/`고` 배열로 정규화 |
| `마감일` | `신청기간_원문` 에서 날짜만 추출. 상시·미정이면 `null` |
| `비용_구분` | 무료 / 소액(약 3만원 이하) / 유료 |
| `과목태그` | `핵심내용`·`활동명` 기반, 복수 가능 |
| `키워드` | **`keywords.json` 사전 안에서만** 선택 (형태 9 + 주제 14 + 특성 7 = 30개 고정) |
| `미국입시_관련도` | 높음 / 보통 / 낮음 |
| `SDC_적합도` | 아래 계산식 |

**키워드를 사전으로 묶은 이유:** 같은 뜻의 표현이 난립해 태그 창이 시끄러워지는 것을 막기 위함.
새 키워드가 필요하면 임의 추가하지 않고 담당자 승인 후 파일에 추가한다.

### 별점 계산식 (0~5점 → ★ 그대로, 최소 ★1)

| 기준 | 배점 |
|---|---|
| 미국 입시 활용도 | 높음 2 / 보통 1 / 낮음 0 |
| 접근성 (검정고시·대안경로 지원 가능) | 가능 1 / 제한적 0 |
| 검증된 주최 (국제·학회·정부·대학) | 해당 1 / 아님 0 |
| 비용 (무료·소액) | 해당 1 / 고비용 0 |

미국입시 관련도가 "낮음"이어도 **사이트에서 빼지 않는다.** 필터로만 구분한다.

---

## 5. 기계 검증 (LLM 이 관여하지 않는 부분)

### 링크 생존 검증 (`check-links.js`)

- 마스터 전체의 `웹사이트`·`포스터_url` 에 GET 요청 (타임아웃 12초, 실패 시 1회 재시도)
- 죽었으면 **데이터를 지우지 않고** 게시상태만 `제외(링크오류: 사유)` 로 변경 → 오탐이면 되돌릴 수 있다
- **신뢰 도메인 예외** — `.go.kr`·`.re.kr`·`.ac.kr` 등 15개 도메인은 404/410 이 아닌 한
  (봇 차단 403·타임아웃·DNS 오류) 게시를 유지하고 "확인 필요"로만 표시.
  *한국 공공사이트가 자동 요청을 막아 멀쩡한 활동이 억울하게 제외되는 것을 막기 위함.*

### 게시 직전 가드레일 (`check-guardrails.js`)

게시 산출물 4개(`docs/data.json`, `docs/index.html`, `master.json`, `pr-body.md`)를 기계적으로 검사.
하나라도 걸리면 **exit 1 로 파이프라인을 멈춘다.**

1. **금칙 명칭** — 특정 학교명 3종을 단어 경계로 탐지
2. **결과 보장 표현** — `(합격|수상|입상|점수)…보장`, `무조건`, `100% 합격`, `반드시 수상` 등.
   "합격·수상 보장이 **아닌**" 같은 부정 고지는 정규식으로 제외
3. **제외 항목 누출** — 게시 제외된 활동이 공개 파일에 새지 않았는지
4. **별점 참고용 고지** — "참고용" 문구가 실제로 있는지

### 안전장치

- 게시 건수가 기준(10건) 미만이면 PR 을 **초안(Draft)** 으로 만들어 실수 머지 방지
- 마감 경과 항목은 마스터에서 제거하되 **아카이브에는 보존** (`docs/archive/YYYY-MM/`)
- 마감일 `null`(상시)은 자동 제거하지 않고 링크 검증이 생존을 관리

---

## 6. 현재 성과 — 실측 (2026-08-27)

**마스터 16건, 전부 게시 상태.** 7월 18건에서 마감 경과 2건 제거, 링크오류 3건 복구.

| 분류 | 건수 | | 별점 | 건수 | | 미국입시 관련도 | 건수 |
|---|---|---|---|---|---|---|---|
| 환경·예술·공익 | 4 | | ★5 | 2 | | 높음 | 5 |
| 과학·공학·수학생명·AI | 4 | | ★4 | 7 | | 보통 | 5 |
| 스포츠·문화·봉사 | 3 | | ★3 | 6 | | **낮음** | **6** |
| 국제·리더십·사회참여 | 3 | | ★2 | 1 | | | |
| 글쓰기·논문·인문사회 | 2 | | ★1 | 0 | | | |

- **마감일 있음 7건 / 상시(null) 9건**
- 비용: 무료 10 · 유료 5 · 소액 1
- 대상: 고 16 · 중 15 · 초 10
- `근거_원문` 16/16 · `지원자격_확인` 16/16 (필수 칸은 100% 채워짐)
- **포스터 0건** · **확인수준 표기 0건**

**수록된 16건 전체**
`amc-2026`, `breakthrough-junior-challenge-2026`, `the-concord-review-2026`,
`hir-academic-writing-contest-2026`, `wharton-investment-competition-2027`,
`world-scholars-cup-2026`, `fss-financial-contest-2026`, `environment-love-contest-2026`,
`seoul-environmental-education-content-contest-2026`, `duke-of-edinburgh-award-korea`,
`1365-volunteer-portal`, `iearn`, `nasa-citizen-science`, `globe-program`,
`roots-and-shoots`, `zooniverse`

---

## 7. 제가 직접 재서 찾아낸 문제 — **여기부터 점검해 주십시오**

아래는 추측이 아니라 **저장소 파일을 실제로 세어 나온 수치**입니다.

### 7-1. 8월 회차의 신규 발굴이 **0건**이다 ← 가장 큰 문제

`data/raw/2026-08.json` 6건을 7월 마스터 id 와 대조한 결과, **6건 전부가 기존 항목의 재확인**이고
새로 발굴된 활동은 하나도 없다.

**그 6건을 증분 수집 규칙별로 분해하면 원인이 더 뚜렷해진다:**

| 증분 규칙 | 8월 실적 |
|---|---|
| ① 마스터에 없는 **새 활동 발굴** | **0건** |
| ② 마감 30일 이내 임박 재확인 | 3건 (Concord Review · 환경사랑공모전 · HIR) |
| ③ `제외` 상태 되살아났는지 재확인 | 3건 (서울시 환경교육 · 1365 · 국제청소년성취포상제) — **3건 전부 게시 복구 성공** |

②③ 은 규칙대로 정확히 작동했다. **①만 통째로 실행되지 않았다.**
2층 발굴 장치(검색 쿼리 세트·정기 순회 소스 5곳·시즌 캘린더)를 다 만들어 뒀는데
산출물에 신규가 0건이라는 것은, 그 장치가 돌지 않았거나 돌았어도 3층 필터를
전부 통과하지 못했다는 뜻이다. **어느 쪽인지 알 방법이 현재 없다** (7-3 참조).

### 7-2. 1층 씨앗 46개 중 상당수가 한 번도 수록되지 않았다

특히 **국내 올림피아드 계열이 통째로 빠져 있다** — KMO·KPhO·KChO·KBO·KOI·USACO·
천문올림피아드·국제수학캥거루·KMC·전국과학전람회. 미국 입시에서 값이 가장 나가는 종류인데 없다.
`John Locke Essay`, `RSI`, `SSP`, `Ross`, `NEC`, `IEO`, `NSDA` 도 마스터에 없다.

수록된 16건 중 **선발성 있는 학술 대회는 5건**(AMC·Concord Review·HIR·Breakthrough Junior·Wharton)뿐이고,
나머지 11건은 상시 참여형(NASA Citizen Science·Zooniverse·GLOBE·iEARN·Roots & Shoots·1365 등)이거나
국내 일반 공모전이다. **상시 9건 · 미국입시 관련도 "낮음" 6건**이라는 분포가 이를 뒷받침한다.

> 시즌 문제일 수 있다(올림피아드는 봄·가을 공고). 다만 시즌 캘린더가 그것을 잡으라고 만든 장치인데
> 잡히지 않았다.

### 7-3. 자기 점검 결과(`coverage.md`)가 **한 번도 생성되지 않았다**

리서처 지시문 §자기 점검 4번이 `data/raw/YYYY-MM.coverage.md` 를 남기라고 요구하는데,
`data/raw/` 에는 `2026-07.json`·`2026-08.json` 두 개뿐이다.

**결과적으로 "무엇을 검토했고 무엇을 왜 버렸는지"가 기록에 없다.** 7-1 의 원인을 못 밝히는 이유가 이것이다.
지시문에만 적혀 있고 **검증 장치가 없어서 조용히 누락됐다.**

### 7-4. 확인수준 표기가 **0건** — 환각 방지 장치가 실제로 안 쓰인다

`[확인수준: 공고요약]`·`[확인수준: 연도불명]` 표기가 16건 중 **0건**이다.
16건 전부 요강 원문까지 확인했을 가능성보다, **표기 규칙이 실행되지 않았을** 가능성이 높다.
큐레이터가 이 표기로 걸러내게 설계돼 있는데 입력이 안 들어오고 있다.

### 7-5. 포스터 파이프라인이 한 번도 작동하지 않았다

`포스터_url` 0건 → `포스터` 0건. 5단계(`fetch-posters.js`)가 매월 돌지만 할 일이 없다.
공개 사이트에 포스터 표시 코드가 있는데 실제로는 텍스트만 나간다.

### 7-6. 링크 검증 이력이 저장소에 안 남는다

GitHub Actions 의 PR `add-paths` 에 `data/reports` 가 빠져 있어,
`data/reports/links-2026-08.json` 이 커밋되지 않았다. 7월 것만 있다.
**매월 무엇이 왜 제외됐는지 추적이 끊긴다.**

### 7-7. 별점 변별력이 약하다

16건 중 **★3~4 에 13건(81%)** 이 몰려 있다. ★1 은 0건.
계산식 4기준 중 "접근성"은 사실상 전원 1점(지원 불가면 애초에 배제되므로),
"검증된 주최"도 대부분 1점(3층 필터를 통과했으므로)이라 **두 기준이 변별에 기여하지 않는다.**
실질적으로는 "미국입시 활용도(0~2) + 비용(0~1)" 3단계 척도로 작동한다.
CLAUDE.md §4 에 "첫 실행 후 별점 분포를 확인해 배점 보정을 검토한다"고 적혀 있으나 보정은 아직 없다.

### 7-8. 자동 실행이 꺼져 있다

`monthly.yml` 의 `schedule` 이 주석 처리돼 수동 실행만 가능하다.
**의도된 것이다**(API 비용 없이 키리스로 운영하려는 목적). 다만 **사람이 잊으면 그 달은 통째로 건너뛴다.**
실제로 7/24 갱신분과 8월 회차가 한 달 넘게 게시되지 않은 채 있었다(원인은 머지 누락이지만 구조는 같다).

---

## 8. 점검자께 여쭙고 싶은 것

1. **발굴이 왜 0건인가** — 프롬프트 설계 문제인가, 3층 필터가 과하게 엄격한가,
   아니면 에이전트가 지시문의 "증분 수집(재조사 안 함)"을 **"신규 발굴도 안 함"**으로
   잘못 읽고 있는가? (§3 의 두 규칙이 한 문서에 붙어 있어 서로를 삼킬 여지가 있어 보인다)
2. **씨앗 46개를 능동적으로 훑는 장치가 필요한가** — 지금은 "발굴하라"는 지시뿐이고
   씨앗 목록을 체크리스트로 강제하는 단계가 없다. 결정적 스크립트로
   "이번 달 미수록 씨앗 목록"을 만들어 리서처에게 입력으로 주는 방식은 어떤가?
3. **`coverage.md` 를 강제하는 방법** — 지시문에 적는 것만으로는 안 지켜졌다.
   가드레일처럼 "파일이 없으면 실패"로 만드는 것이 맞는가?
4. **별점 계산식 재설계** — 변별력 없는 두 기준을 빼고 선별성(합격률·경쟁률)·
   국제 인지도 같은 축을 넣는 것이 나은가? 아니면 별점 자체가 과한 단순화인가?
5. **상시형 활동의 비중** — 누구나 언제든 참여 가능한 활동(Zooniverse 등)이 절반이다.
   ECA 목록으로서 적절한 비중인가, 선발형 중심으로 재편해야 하는가?
6. **검색 쿼리 세트의 실효성** — 국문 위주 쿼리로 국제 대회를 얼마나 잡을 수 있는가?
   영문 쿼리가 대분류당 1개뿐이다.

---

## 부록 — 관련 파일 위치

| 내용 | 파일 |
|---|---|
| 공통 규칙·스키마·별점 계산식 | `CLAUDE.md` |
| 소스 3층 전략 | `data/sources.json` |
| 키워드 사전 (30개 고정) | `data/keywords.json` |
| 리서치 지시문 | `.claude/agents/researcher.md` |
| 큐레이션 지시문 | `.claude/agents/curator.md` |
| 병합·링크검증·가드레일 | `scripts/merge.js`, `scripts/check-links.js`, `scripts/check-guardrails.js` |
| 공개 필드 정의 | `scripts/build.js` 의 `PUBLIC_FIELDS` |
| 자동 실행 워크플로 | `.github/workflows/monthly.yml` |
