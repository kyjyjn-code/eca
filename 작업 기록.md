# EC Researcher — 작업 기록

이 폴더에서 한 일만 적는다. 교사앱에 등록하는 부분의 계약은 여기에 쓰지 않는다
(정본 위치는 `CLAUDE.md` 맨 위 포인터 참조).

---

## 2026-08-27 — 공개 `data.json` 에 `갱신일` 추가 + 파이프라인 점검

### 1. 파이프라인 점검 — 8월 회차는 **돌았다**

"8월 회차가 안 돌았다"고 보였던 것은 로컬 기준이었다. 실제 상태는 다르다.

| 확인한 것 | 결과 |
|---|---|
| `main` 의 `monthly.yml` | `schedule: '0 0 1 * *'` **살아 있음** |
| 8월 실행 흔적 | `origin/eca/update-2026-08` 존재 · 커밋 `0fed473` · **2026-08-01 08:10 UTC** |
| 그 산출물 | `master.json` 16건 전부 `게시` · `docs/data.json` 16건 |
| 게시 건수 게이트(10건) | 통과 — Draft 아님, 정상 PR |

**원인: 자동화가 멈춘 것이 아니라, 만들어진 PR 이 머지되지 않아 멈춰 있다.**
7/24 갱신분(`a11y/filter-keyboard-url-state`)도 같은 이유로 `main` 에 없다.
그래서 배포본이 7/10 자에 멈춰 있었다.

> 주의로 남긴다 — 현재 작업 브랜치의 커밋 `2c4940f` 에 **cron 주석 처리**가 들어 있다.
> 이 브랜치를 머지하면 매월 자동 실행이 꺼지고 수동 버튼만 남는다.

**미커밋으로 남아 있는 것(내 작업 범위 밖이라 손대지 않음):**
`scripts/check-guardrails.js` 가 미추적인데 워킹트리의 `monthly.yml` 에는 이미
"가드레일 검사" 스텝이 들어 있다. **이 둘은 짝이다** — 스텝만 커밋되고 스크립트가
없으면 CI 가 그 자리에서 깨진다. 커밋할 때 반드시 함께 넣는다.

### 2. `PUBLIC_FIELDS` 에 `갱신일` 한 칸 추가

`scripts/build.js` 1줄. 공개 `data.json` 이 15칸 → 16칸.
`지원자격_확인`·`근거_원문` 은 계약대로 공개하지 않는다.

**값으로 확인한 것** (빌드 성공은 확인이 아니다):

- `docs/data.json` **15건 전부**에 `갱신일` 있음 (전부 `2026-07-07`)
- 빌드 전/후 나머지 15칸 전 항목 심층 대조 — **차이 0**
- 내부 메모 누출 없음
- `node scripts/check-guardrails.js` **통과**
- `index.html` 화면 표시 변화 없음 (렌더링 로직이 `갱신일` 을 참조하지 않는다)

커밋 `c557f0b` · 브랜치 `a11y/filter-keyboard-url-state` 푸시 완료.

### 3. 8월 회차 병합 — `docs/` 충돌은 재빌드로 해결

"8월분 먼저, 오늘 것 나중" 순서를 브랜치 안에서 처리했다.
`origin/eca/update-2026-08` 을 병합하니 `docs/data.json` · `docs/index.html` 이 충돌했다.
**이 둘은 `master.json` 에서 파생되는 산출물이므로 충돌 마커를 손으로 풀지 않고
`node scripts/build.js` 로 다시 만들었다.** 아카이브(`docs/archive/2026-08/`)도
본편과 어긋나 있어 재빌드본으로 맞췄다.

병합 후 확인:

- `master.json` 16건 전부 `게시` · 갱신일 전부 있음 (`2026-08-01` 6건 · `2026-07-07` 10건)
- 충돌 마커 잔존 0 · 8월 원본과 대조해 **갱신일 외 15칸 차이 0**
- `check-guardrails.js` 통과

### 4. 로컬 미리보기 → 배포 (사용자 확인 후)

사용자 요청으로 **배포 전에 로컬 HTML 을 먼저 보여주는 방식**으로 바꿨다
(`python -m http.server 8788 --directory docs`). 확인한 것:

- 데이터 16건 · 화면 카드 13건 (마감 지난 3건은 기본 숨김 — 정상 동작)
- 390px 가로 스크롤 없음 · 카드 레이아웃 정상
- 콘솔 오류는 `favicon.ico` 404 하나뿐 (기능 무관)

승인 후 `git push origin HEAD:main` (fast-forward, 충돌 없음).

### 5. 배포 확인 — **값으로 받아서 확인함**

`curl https://kyjyjn-code.github.io/eca/data.json` 실측:

| 항목 | 배포 전 | 배포 후 |
|---|---|---|
| 크기 | 15,260 bytes (7/10 자) | **17,258 bytes** |
| 건수 | 15건 | **16건** |
| 칸 수 | 15칸 | **16칸** |
| `갱신일` | 없음 | **16/16 전부 있음** |

- 계약 §3 대응표 16칸과 **키 이름·순서 완전 일치**
- 내부 메모(`지원자격_확인`·`근거_원문`) 누출 없음
- 실제 사이트 화면도 로컬과 동일하게 렌더 · "참고용" 고지 있음

**밀려 있던 7/24 갱신분과 8월 회차가 이때 함께 반영됐다.**

### 6. 남아 있는 것 (다음 세션)

- `scripts/check-guardrails.js` 가 여전히 **미추적**인데 워킹트리 `monthly.yml` 에는
  "가드레일 검사" 스텝이 있다. **짝이므로 반드시 함께 커밋한다.**
- 넓은 화면(1280px 3열)에서 카드 하나의 마감일이 두 줄로 끊기고 「임박」 배지가 눌린다.
  휴대폰에서는 정상. 오늘 작업과 무관한 기존 건.

### 7. 이 환경에서 걸린 것 (다음에 반복하지 않도록)

- Git Bash 에서 `git show "브랜치:경로"` 의 콜론이 Windows 경로로 변환된다.
  → `MSYS_NO_PATHCONV=1` 을 앞에 붙인다.
- `/tmp` 가 `C:\tmp` 로 잡혀 없다. 임시 파일은 스크래치패드 경로를 쓴다.

- `gh` CLI 가 없고 토큰도 없어 **PR 생성·머지는 못 한다.** 다만 git 자격증명은 있어
  `git push origin HEAD:main` 으로 **배포는 할 수 있다.**
- 작업 폴더에서 `rm` 이 차단된다. 임시 파일은 만들지 말고, 생겼으면 스크래치패드로 옮긴다.

---

## 이 프로젝트의 배포 방식 (2026-08-27 사용자 지정)

설계서 §7 은 "PR 로 만들어 담당자가 승인(머지)" 이지만, **승인 지점을 GitHub PR 에서
로컬 미리보기로 옮긴다.** PR diff 보다 실제 화면을 보는 쪽이 실질적인 확인이기 때문이다.

1. 파이프라인을 돌려 `docs/` 를 만든다.
2. **로컬 서버로 화면을 먼저 보여준다** — `python -m http.server 8788 --directory docs`.
3. 사용자가 보고 수정을 요청하면 로컬에서 고치고 다시 보여준다.
4. **"배포해주세요" 를 들으면 그때 `main` 에 푸시한다.**
5. 푸시 후 `curl` 로 공개 `data.json` 을 **받아서 값으로** 확인한다 — 푸시가 곧 반영은 아니다.

가드레일 검사(`check-guardrails.js`)는 2단계 전에 반드시 통과시킨다.
