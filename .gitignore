node_modules/
*.log
.DS_Store
Thumbs.db

# 임시 파일
scratchpad/
*.tmp

# 파이프라인 생성물 — 매 실행 재생성된다.
# 가드레일 검사 대상이라 낡은 회차 본문이 남아 있으면 검사를 오염시킨다.
pr-body.md

# 내부 논의 문서 — 교사 실명·내부 논의가 들어 있어 공개 저장소에 올리지 않는다.
26년 8월 27일 - 리서치 파이프라인 개선/
